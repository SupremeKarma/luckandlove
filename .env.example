# Public Supabase credentials
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only keys (NEVER prefix with NEXT_PUBLIC)
# Used for admin tasks, bypassing RLS.
SUPABASE_SERVICE_ROLE_KEY=

# Optional internal admin token for protecting custom API routes
INTERNAL_ADMIN_TOKEN=
